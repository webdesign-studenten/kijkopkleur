<script>
    require([
        'jquery',
        'Magento_Ui/js/modal/alert',
        'prototype'
    ], function(jQuery, alert){
        function shipment_sync() {
            params = {
            };

            new Ajax.Request('<?php echo $block->getAjaxUrl() ?>', {
                loaderArea:     true,
                asynchronous:   true,
                parameters:     params,
                onSuccess: function(transport) {
                    alert({
                        content: 'Shipment Sync is completed.'
                    })
                }
            });
        }

        jQuery('#shipmentSync').click(function () {
            shipment_sync();
        });
    });
</script>

<?php echo $block->getButtonHtml() ?>