<script>
    require([
        'jquery',
        'Magento_Ui/js/modal/alert',
        'prototype'
    ], function(jQuery, alert){
        function sales_sync() {
            params = {
            };

            new Ajax.Request('<?php echo $block->getAjaxUrl() ?>', {
                loaderArea:     true,
                asynchronous:   true,
                parameters:     params,
                onSuccess: function(transport) {
                    alert({
                        content: 'Order Sync is completed.'
                    })
                }
            });
        }

        jQuery('#salesSync').click(function () {
            sales_sync();
        });
    });
</script>

<?php echo $block->getButtonHtml() ?>